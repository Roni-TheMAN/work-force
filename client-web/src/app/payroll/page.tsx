import { WorkspacePlaceholder } from "@/pages/workspace-placeholder";

export function PayrollPage() {
  return (
    <WorkspacePlaceholder
      title="Payroll"
      description="Payroll reporting will live here. The route, sidebar navigation, and shared workspace shell are now active."
    />
  );
}
