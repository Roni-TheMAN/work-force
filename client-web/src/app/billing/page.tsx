import { WorkspacePlaceholder } from "@/pages/workspace-placeholder";

export function BillingWorkspacePage() {
  return (
    <WorkspacePlaceholder
      title="Billing"
      description="Billing controls will live here. The route, sidebar navigation, and shared workspace shell are now active."
    />
  );
}
