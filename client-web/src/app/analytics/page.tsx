import { WorkspacePlaceholder } from "@/pages/workspace-placeholder";

export function AnalyticsPage() {
  return (
    <WorkspacePlaceholder
      title="Analytics"
      description="Expanded analytics will live here. The route, sidebar navigation, and shared workspace shell are now active."
    />
  );
}
