import { WorkspacePlaceholder } from "@/pages/workspace-placeholder";

export function SettingsPage() {
  return (
    <WorkspacePlaceholder
      title="Settings"
      description="Workspace settings will live here. The route, sidebar navigation, and shared workspace shell are now active."
    />
  );
}
