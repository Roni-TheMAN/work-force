import { WorkspacePlaceholder } from "@/pages/workspace-placeholder";

export function PropertiesPage() {
  return (
    <WorkspacePlaceholder
      title="Properties"
      description="Property management will live here. The route, sidebar navigation, and shared workspace shell are now active."
    />
  );
}
