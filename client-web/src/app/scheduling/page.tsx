import { WorkspacePlaceholder } from "@/pages/workspace-placeholder";

export function SchedulingPage() {
  return (
    <WorkspacePlaceholder
      title="Scheduling"
      description="Scheduling workflows will live here. The route, sidebar navigation, and shared workspace shell are now active."
    />
  );
}
