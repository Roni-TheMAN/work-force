import { WorkspacePlaceholder } from "@/pages/workspace-placeholder";

export function EmployeesPage() {
  return (
    <WorkspacePlaceholder
      title="Employees"
      description="Employee management will live here. The route, sidebar navigation, and shared workspace shell are now active."
    />
  );
}
